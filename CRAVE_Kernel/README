# Instruction to compile and run the kernel for CRAVE ( this is required for ODROID-XU4)

### Install dependencies
Install the dependencies on your ODROID board from [here](https://wiki.odroid.com/odroid-xu4/software/building_kernel).

### Compile the kernel

sudo ./CRAVEmake.sh

### Verification

If memory and GPU devfreq is on ondemand governor, you should see utilization sysfs.


